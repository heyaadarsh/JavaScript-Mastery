// Variables

// Variables can store some information.
// We can use that information later.
// We can change that information later.

// We have to use 'var' keyword to declare a variable in JS, but without using 'var' we can also declare the variable for preventing this we have to use the below line:
"use strict";

// Declairing a variable
var myName = "Aadarsh Giri";
// Variables Names are case sensitive.

// Using Variable
console.log(myName);

// Changing Variable value
myName = "Ayati";

// Printing again
console.log(myName);